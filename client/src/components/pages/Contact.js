import React from 'react';

import Default from '../layout/Default';

export default () => (
    <Default>
        <div className="container">
            <h1>Contact</h1>
        </div>
    </Default>
);